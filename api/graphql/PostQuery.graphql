query PostQuery($postId: ID!) {
    postDetail(id: $postId) {
        text
        category {
            name
        }
        keywords {
            name
        }
    },
    postList(keywordId: 3, categoryId: 1) {
        name,
        category {
            name
        }
        keywords {
            name
        }
    }
}