# This file was generated based on ".graphqlconfig". Do not edit manually.

schema {
    query: Query
    mutation: Mutation
}

type CategoryType {
    id: ID!
    name: String!
    posts: [PostType!]!
}

type ErrorType {
    field: String!
    messages: [String!]!
}

type KeywordType {
    id: ID!
    name: String!
    posts: [PostType!]!
}

type Mutation {
    createPost(input: PostMutationInput!): PostMutationPayload
    updatePost(input: PostMutationInput!): PostMutationPayload
}

type PostMutationPayload {
    clientMutationId: String
    errors: [ErrorType]
    post: PostType
}

type PostType {
    category: CategoryType!
    dateModified: DateTime!
    datePublished: DateTime!
    id: ID!
    image: String
    keywords: [KeywordType!]!
    name: String!
    text: String
    writtenBy: UserType
}

type Query {
    categoryDetail(id: ID!): CategoryType
    categoryList: [CategoryType]
    hello: String
    keywordDetail(id: ID!): KeywordType
    keywordList: [KeywordType]
    postDetail(id: ID!): PostType
    postList(categoryId: ID, keywordId: ID): [PostType]
    userDetail(username: String!): UserType
}

type UserType {
    firstName: String!
    lastName: String!
    posts: [PostType!]!
    "Required. 150 characters or fewer. Letters, digits and @/./+/-/_ only."
    username: String!
}

"""

The `DateTime` scalar type represents a DateTime
value as specified by
[iso8601](https://en.wikipedia.org/wiki/ISO_8601).
"""
scalar DateTime

input PostMutationInput {
    category: ID!
    clientMutationId: String
    id: ID
    image: String
    keywords: [ID]!
    name: String!
    text: String!
    writtenBy: ID!
}
